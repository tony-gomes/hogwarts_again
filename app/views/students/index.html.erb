<h1>Students</h1>

<section>
  <% @students.each do |student| %>
    <div class="student-block">
      <h3> <%= student.name %>: <% student.professors.count %> </h3>
    </div>
  <% end %>
</section>
